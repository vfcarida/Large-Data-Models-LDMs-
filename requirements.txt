# =============================================================================
# Large Data Models (LDMs) — Python Dependencies
# =============================================================================
# Versões fixadas para reprodutibilidade. Atualize com cautela.
# =============================================================================

# ---------------------------------------------------------------------------
# Core Deep Learning Framework
# ---------------------------------------------------------------------------
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ---------------------------------------------------------------------------
# Training Framework & Distributed
# ---------------------------------------------------------------------------
pytorch-lightning>=2.1.0       # Training loops, logging, distributed training
torchmetrics>=1.2.0            # Production-grade metrics (AUC, F1, etc.)

# ---------------------------------------------------------------------------
# LLM / LDM Tools and Adaptation (PeFT)
# ---------------------------------------------------------------------------
transformers>=4.36.0           # Tokenizers, pre-trained models
peft>=0.7.0                    # LoRA, QLoRA for parameter-efficient fine-tuning
accelerate>=0.25.0             # Multi-GPU/TPU training utilities

# ---------------------------------------------------------------------------
# Configuration Management
# ---------------------------------------------------------------------------
hydra-core>=1.3.2              # Declarative config management
omegaconf>=2.3.0               # YAML config parsing (used by Hydra)

# ---------------------------------------------------------------------------
# Data Processing
# ---------------------------------------------------------------------------
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.1
pyarrow>=14.0.0                # Parquet file support (fast I/O)
scikit-learn>=1.3.0            # Train/test splits, preprocessing utilities

# ---------------------------------------------------------------------------
# GPU-Accelerated Data Processing (Optional)
# ---------------------------------------------------------------------------
# NOTE: cuDF requires NVIDIA GPU and specific CUDA installation.
# Install separately via conda/nvidia index:
#   conda install -c rapidsai -c conda-forge -c nvidia cudf=24.02
# cudf-cu12                    # Uncomment if RAPIDS is available

# ---------------------------------------------------------------------------
# Experiment Tracking & Visualization
# ---------------------------------------------------------------------------
wandb>=0.16.0                  # Weights & Biases experiment tracking
tensorboard>=2.15.0            # TensorBoard logging (alternative)

# ---------------------------------------------------------------------------
# Visualization & Reporting
# ---------------------------------------------------------------------------
matplotlib>=3.8.0              # Plotting training curves and metrics
seaborn>=0.13.0                # Statistical data visualization

# ---------------------------------------------------------------------------
# Development Utilities
# ---------------------------------------------------------------------------
rich>=13.0.0                   # Beautiful terminal output and progress bars
tqdm>=4.66.0                   # Progress bars for data processing
